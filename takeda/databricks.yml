# This is a Databricks asset bundle definition for takeda.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: takeda
  uuid: d57f6b0e-ba7c-428f-8a83-0d645bd4e41d

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The target schema to use
  full_catalog:
    description: The full catalog to use
  tgt:
    description: The target env to use
    


targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-15208c23-e12e.cloud.databricks.com
    variables:
      catalog: labuser13204995_1766028921
      schema: default
      tgt: dev
      full_catalog: labuser13204995_1766028921_1_${var.target}
  prod:
    mode: production
    workspace:
      host: https://dbc-15208c23-e12e.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/labuser13204995_1766028921@vocareum.com to make sure we only have a single copy.
      root_path: /Workspace/Users/labuser13204995_1766028921@vocareum.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: labuser13204995_1766028921
      schema: default
      tgt: dev
      full_catalog: labuser13204995_1766028921_3_${var.tgt}
    permissions:
      - user_name: labuser13204995_1766028921@vocareum.com
        level: CAN_MANAGE
